import HeroSection from "../components/HeroSection.tsx";
import {Box} from "@mui/material";

const Home = () => {
    return (
        <Box display={"flex"}>
            <HeroSection/>
        </Box>
    )
}

export default Home;